module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: ["Space Grotesk", "sans-serif"], // Override Tailwind's default sans
      },
    },
  },
  // other Tailwind settings
};
